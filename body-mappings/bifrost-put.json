{
    "id"   : "$input.json('$.id')",
    "name" : "$input.json('$.name')",
    "ip"   : "$input.json('$.ip')",
    "location": {
        "latitude" : "$input.json('$.location.lat')",
        "longitude": "$input.json('$.location.lon')"
    }
}